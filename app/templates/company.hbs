<div class="col-sm-12 projects-nav">
	<h4>Projects:</h4>
	<ul class="nav nav-pills">
		{{#each projects}}
			{{#link-li}}{{link-to title 'company.project' slug}}{{/link-li}}
		{{/each}}
	</ul>
</div>

{{outlet}}
